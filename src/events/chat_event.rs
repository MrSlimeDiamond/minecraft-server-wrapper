use crate::Player;
use crate::send_cmd;

pub fn fire_event(player: Player, content: &str) {
    // TODO: Add your code here :)
    // Examples:
    //
    // send_cmd!("say {}: {}", player.name, content);
}