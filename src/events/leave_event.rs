use crate::send_cmd;
use crate::Player;

pub fn fire_event(player: Player, reason: &str) {
    // TODO: Add your code here :)
    // Examples:
    //
    // send_cmd!("say Bye, {}: {}", player.name, reason);
}